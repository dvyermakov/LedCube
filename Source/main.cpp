#include "stm32f10x.h"
#include "stm32f10x_gpio.h"     // ???? ? ????????? ?????????? ??????? ???????????
#include "stm32f10x_rcc.h"      // ?????????? ?????????????
#include <stdint.h>             // ?????????? ???? ??????, ?????? ?????? int, char ? ???? ????????
#include <cube.h>
#include <timer.h>

void init(void){

}

int main(void){	
	init();
	InitTimers();
	Cube myCube;
	bool led_state = false;
	ResetTimer(T_BLINK_Off);
	while (1){
		if (led_state){
			if (GetTimer(T_BLINK_On) >= sec){
//				setLedState(0, false);
//				setRow(0);
//				setFloor(0);
//				led_state = false;
				ResetTimer(T_BLINK_Off);
			}
		}
		else{
			if (GetTimer(T_BLINK_Off) >= sec){
//				setLedState(0, true);
//				setRow(0);
//				setFloor(0);
//				led_state = true;
				ResetTimer(T_BLINK_On);
			}
		}
	}	
	return 0;
}
